<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dashboard">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mirrorlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mirrorlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".mirrorsAvailable"></activity>
        <activity android:name=".Clock" />
        <activity android:name=".Calendar" />
        <activity android:name=".CurrentWeather" />
        <activity android:name=".Compliments" />
        <activity android:name=".Account" />
        <activity android:name=".Notifications" />
        <activity android:name=".GeneralSetting" />
        <activity android:name=".Newsfeed" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Welcome">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>